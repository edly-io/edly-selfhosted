# Install survey API app (if not installed before)
RUN stat /mnt/survey_api 2> /dev/null || pip install -e "git+https://github.com/edly-io/edly-selfhosted.git@{{ EDLY_SELFHOSTED_GIT_BRANCH }}#egg=survey_api&subdirectory=survey_api"
